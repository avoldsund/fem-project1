function [I, J] = test_integrals_quadrature_2d()

fun1 = @(x, y) log(x + y);
xmin1 = 1;
xmax1 = 3;
ymin1 = @(x) 1/2 * x - 1/2;
ymax1 = @(x) x - 1;

I = integral2(fun1, xmin1, xmax1, ymin1, ymax1);

fun2 = @(x, y) x^2 * y^2 + x + y;
xmin2 = @()
ymin2 = 0;
ymax2 = 3;


J = integral2(fun2);
end